sudo apt update

sudo apt install docker-compose docker.io wget -y

mkdir wg-access-server

cd wg-access-server

wget https://raw.githubusercontent.com/Pqko0/wg-vpn/main/dk -O docker-compose.yml

sudo docker-compose up -d
